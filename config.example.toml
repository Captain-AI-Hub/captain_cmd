[mcp_servers]
content = '''
{
    "mcpServers": {
        "example-stdio-mcp-name": {
            "transport": "stdio", 
            "command": "uv/npx", 
            "args": ["script"]
        },
        "example-tcp-mcp-name": {
            "transport": "streamable_http",
            "url": "http://mcp-server:port/mcp"
        }
     }
}
'''

# Major Agent and Sub Agents
# don't add tools to major agent, add tools to sub agents.
[model_config]
[model_config.major_agent]
    model_name = ""
    api_key = ""
    base_url = ""
    system_prompt = '''
    '''
    timeout = 600       
    max_retries = 3     
[model_config.vlm_agent]
    model_name = ""
    api_key = ""
    base_url = ""
    system_prompt = '''
    '''
    inside_tools = ["read_image"]
    timeout = 600       
    max_retries = 3     
[model_config.sub_agent_1]
    model_name = ""
    api_key = ""
    base_url = ""
    system_prompt = '''
    '''
    mcp_tools = ["example-stdio-mcp-name", "..."]
    inside_tools = ["..."]
    timeout = 600       
    max_retries = 3     
[model_config.sub_agent_2]
    model_name = ""
    api_key = ""
    base_url = ""
    system_prompt = '''
    '''
    mcp_tools = ["example-tcp-mcp-name", "..."]
    inside_tools = ["..."]
    timeout = 600       
    max_retries = 3     

# Tavily API Key
[tavily_config]
tavily_api_key = ""

# Prompt Templates
[prompt_templates]

[prompt_templates.init]
prompt = '''

'''

[prompt_templates.example]
args = ["example_arg1", "example_arg2"]
prompt = '''
use {example_arg1} and {example_arg2} to ...
'''

[prompt_templates.fetch_url]
args = ["url"]
prompt = '''
Fetch the content from {url}.
'''
